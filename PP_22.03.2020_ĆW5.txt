#include <iostream>
#include <cstdio>
using namespace std;

int main() {
  //Zrealizuj program, który dla dowolnej liczby naturalnej, okreœli iloœæ cyfr tej liczby.
  
  unsigned int liczba;
  int suma = 0;
  int licznik = 0;
  cout<<"WprowadŸ dowoln¹ liczbê ca³kowit¹"<<endl;
  cin>>liczba;
  while (liczba > 0)
  {
    suma += liczba%10;
    liczba/=10;
    licznik+=1;
  }
  cout<<licznik;
}